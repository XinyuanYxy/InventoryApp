let express,mongoose,cors,morgan,readdirSync;_6ec‍.w("express",[["default",["express"],function(v){express=v}]]);_6ec‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_6ec‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_6ec‍.w("morgan",[["default",["morgan"],function(v){morgan=v}]]);_6ec‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);




require('dotenv').config();

//app
const app = express();

//db
mongoose
	.connect(process.env.MONGO_URI, {
		userNewUrlParser: true,
		useUnifiedTopology: true,
		useFindAndModify: false,
		useCreateIndex: true,
	})
	.then(() => console.log('DB cloud connected'));

//middleware
app.use(cors());
app.use(morgan('dev'));
readdirSync('./routes').map((route) =>
	app.use('/api', require(`./routes/${route}`))
);
//routes

const port = process.env.PORT || 8000;
app.listen(port, () => console.log(`The server is running on port ${port}`));
